services:
  mediawiki-mcp:
    build: .
    container_name: mediawiki-mcp
    env_file:
      - .env
    environment:
      - MEDIAWIKI_MCP_HOST=0.0.0.0
    ports:
      - "8009:8009"
    restart: unless-stopped
